= form_with(model: cart_item, local: true) do |form|
  - if cart_item.errors.any?
    #error_explanation
      %h2
        = pluralize(cart_item.errors.count, "error")
        prohibited this cart_item from being saved:
      %ul
        - cart_item.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :cart_id
    = form.text_field :cart_id, id: :cart_item_cart_id
  .field
    = form.label :item_id
    = form.text_field :item_id, id: :cart_item_item_id
  .actions
    = form.submit
