= form_with(model: cart, local: true) do |form|
  - if cart.errors.any?
    #error_explanation
      %h2
        = pluralize(cart.errors.count, "error")
        prohibited this cart from being saved:
      %ul
        - cart.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :user_id
    = form.text_field :user_id, id: :cart_user_id
  .actions
    = form.submit
